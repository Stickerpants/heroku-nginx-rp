{
	"name": "Reverse Proxy",
	"description": "Spin up a reverse proxy quickly on Heroku",
	"keywords": [
		"reverse proxy",
		"nginx"
	],
	"repository": "https://github.com/Stickerpants/heroku-nginx-rp",
	"buildpacks": [
		{
			"url": "https://github.com/Stickerpants/heroku-buildpack-nginx.git"
		}
	],
	"env": {
		"UPSTREAM_SERVER": {
			"description": "URL of upstream server to which all requests should be proxied"
		}
	},
	"stack": "heroku-18"
}
